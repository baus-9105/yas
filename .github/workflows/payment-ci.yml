name: Payment Service CI

on:
  push:
    branches: [ "**" ]
    paths:
      - "payment/**"
      - "common-library/**"
      - ".github/workflows/payment-ci.yml"
      - "pom.xml"

  pull_request:
    branches: [ "main" ]
    paths:
      - "payment/**"
      - "common-library/**"
      - ".github/workflows/payment-ci.yml"
      - "pom.xml"
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      checks: write
      pull-requests: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Build with Maven
        run: mvn clean install -pl payment -am -DskipTests -Drevision=1.0-SNAPSHOT
        
      - name: Test with Maven
        run: mvn verify -pl payment -am -Drevision=1.0-SNAPSHOT

      - name: Test Results
        uses: dorny/test-reporter@v1
        if: success() || failure()
        with:
          name: Payment-Service-Unit-Test-Results
          path: "payment/**/surefire-reports/*.xml"
          reporter: java-junit
      
      - name: Add coverage report to PR
        uses: madrapps/jacoco-report@v1.6.1
        with:
          paths: ${{ github.workspace }}/payment/target/site/jacoco/jacoco.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 70
          min-coverage-changed-files: 70
          title: 'Payment Coverage Report'
          update-comment: true
